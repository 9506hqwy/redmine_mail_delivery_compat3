{
    "name": "redmine_mail_delivery_compat3",
    "scripts": {
        "check": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint --write"
    },
    "devDependencies": {
        "@biomejs/biome": "2.2.5",
        "@playwright/test": "^1.53.0",
        "@types/node": "^24.0.1"
    }
}
